@extends('layouts.main') @section('title','FIMP') @section('content')<div class="d-flex justify-content-between mb-24"><h5>FIMP</h5><a class="btn btn-primary-600" href="{{ route('fimp.create') }}">Buat FIMP</a></div>@include('employee.partials.alert')<div class="card"><div class="card-body table-responsive"><table class="table"><thead><tr><th>Periode</th><th>Pengganti</th><th>Keperluan</th><th>Status</th><th></th></tr></thead><tbody>@forelse($requests as $item)<tr><td>{{ $item->tanggal_awal->format('d M Y') }} - {{ $item->tanggal_akhir->format('d M Y') }}</td><td>{{ $item->penggantiKaryawan->nama_karyawan }}</td><td>{{ $item->keperluan }}</td><td>{{ ->status_approval === 'Pending' ? 'Menunggu' : (->status_approval === 'Approve' ? 'Disetujui' : 'Ditolak') }}</td><td>@if($item->status_approval==='Pending')<a href="{{ route('fimp.edit',$item) }}">Ubah</a><form class="d-inline" method="POST" action="{{ route('fimp.destroy',$item) }}">@csrf @method('DELETE')<button class="btn btn-link text-danger">Hapus</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center">Belum ada FIMP.</td></tr>@endforelse</tbody></table></div></div>@endsection

